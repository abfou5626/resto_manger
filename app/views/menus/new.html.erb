<% content_for :title, "New menu" %>

<div class="md:w-2/3 w-full mx-auto mt-10">
  <h1 class="font-bold text-4xl mb-6">New menu</h1>

  <%= render "form", menu: @menu %>

  <%= link_to "Back to menus", menus_path, class: "inline-block mt-4 text-gray-600 hover:text-gray-800" %>
</div>
